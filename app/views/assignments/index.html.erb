<div class="row">
  <div class="col-md-3"><h2 class="curriculum-heading">Prework</h2></div>
</div>
<div class="row message">
  <div class="col-md-7">
    <p><span class="prework-important">Prework is mandatory. You will not be allowed to start the program if you do not complete the Prework.</span> Give yourself plenty of time to complete the prework. If you're stuck and you just can't figure something out, reach out to your instructor <b><%= @cohort.instructor.full_name  %></b> (<%= mail_to @cohort.instructor.email %>).</p>
    <p>View assignments below.</p>
  </div>
  <div class="col-md-5">
    <div class="white-due">
      <table class="table">
        <tr>
          <td>Week 1 Prework Assignment</td>
          <td><span class="warning-text">Due: <%= (@cohort.start_date + 6).strftime("%A,  %b %d, %Y") %></span></td>
        </tr>
        <tr>
          <td>Week 2 Prework Assignment</td>
          <td><span class="warning-text">Due: <%= (@cohort.start_date + 13).strftime("%A,  %b %d, %Y") %></span></td>
        </tr>
        <tr>
          <td>Week 3 Prework Assignment</td>
          <td><span class="warning-text">Due: <%= (@cohort.start_date + 19).strftime("%A,  %b %d, %Y") %></span></td>
        </tr>
      </table>
    </div>
  </div>
</div>
<div class="row">
  <div class="col-md-12">

    <div class="white">
      <tabset justified="true">
        <tab heading="Prework Week 1" active="<%= @tab_index %> === 1">
          <div class="tab-heading">
          </div>
          <%= render 'table', week: 1 %>
        </tab>
        <tab heading="Prework Week 2" active="<%= @tab_index %> === 2">
          <div class="tab-heading">
          </div>
        <%= render 'table', week: 2 %>
        </tab>
        <tab heading="Prework Week 3" active="<%= @tab_index %> === 3">
          <div class="tab-heading">
        <%= render 'table', week: 3 %>
        </tab>
      </tabset>
    </div>
  </div>

</div>


